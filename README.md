# Number Garden（数字花园）

面向 10-12 岁小学生的互动式数学认知训练 MVP。它强调数字关系、位值结构、进位/退位中的单位转换，而不是刷题速度。

## 功能

- 数字探索：输入数字后生成关系树，点击枝叶查看解释。
- 进位实验室：用“十”和“一”的积木展示 `27 + 18` 中 `10个一 = 1个十`。
- 退位实验室：用单位转换解释 `42 - 18`，强调退位不是借东西。
- 今日挑战：鼓励孩子发现多个等价关系，并把探索记录保存在 LocalStorage。


## 最简单打开方式

如果你只想看我写好的网页长什么样，请直接双击根目录里的：

```text
打开这个-Number-Garden.html
```

它是完整静态网页，不需要安装 npm，也不需要运行命令。

## 快速预览 HTML

如果只是想先看效果，可以直接在浏览器打开根目录下的 `preview.html`。这个文件是零依赖静态预览版，适合在 npm 依赖无法安装时查看 MVP 交互方向。

## 运行 React 版本

```bash
npm install
npm run dev
```

## 构建

```bash
npm run build
```
